<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ meal_plan.title }}</title>  <!-- Changed from mealPlan to meal_plan -->
    <style>
        body {
            font-family: sans-serif;
            margin: 0;
            padding: 0;
            color: #333;
        }
        .header {
            background: linear-gradient(135deg, #1abc9c, #16a085);
            color: white;
            padding: 20px;
            text-align: center;
            margin-bottom: 30px;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
            font-weight: bold;
        }
        .container {
            padding: 0 30px;
        }
        .summary-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        .summary-table th, .summary-table td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
        }
        .summary-table th {
            background-color: #f8f9fa;
            width: 30%;
            font-weight: 600;
        }
        .meal-section {
            margin-bottom: 25px;
            border: 2px solid #1abc9c;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 3px 10px rgba(0,0,0,0.1);
        }
        .meal-section h2 {
            color: #2c3e50;
            border-bottom: 2px solid #1abc9c;
            padding-bottom: 8px;
            margin-top: 0;
            font-size: 20px;
            font-weight: 600;
        }
        .meal-info {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            margin-top: 15px;
        }
        .meal-info p {
            margin: 8px 0;
        }
        .footer {
            text-align: center;
            margin-top: 40px;
            padding-top: 20px;
            border-top: 2px solid #eee;
            color: #666;
            font-size: 12px;
        }
        .logo {
            text-align: center;
            margin-bottom: 20px;
            font-size: 18px;
            font-weight: bold;
            color: #1abc9c;
        }
    </style>
</head>
<body>
<div class="logo">FitFlow Meal Plan</div>

<div class="header">
    <h1>{{ meal_plan.title }}</h1>  <!-- Changed from mealPlan to meal_plan -->
    <p>Generated for {{ meal_plan.user.name ?? 'User' }}</p>  <!-- Changed from mealPlan to meal_plan -->
    <p>Day: {{ meal_plan.day }}</p>  <!-- Changed from mealPlan to meal_plan -->
</div>

<div class="container">
    <h2>Plan Summary</h2>
    <table class="summary-table">
        <tr>
            <th>Target Day</th>
            <td>{{ meal_plan.day }}</td>  <!-- Changed from mealPlan to meal_plan -->
        </tr>
        <tr>
            <th>Total Calories</th>
            <td>{{ meal_plan.totalCalories }} kcal</td>  <!-- Changed from mealPlan to meal_plan -->
        </tr>
        <tr>
            <th>User Goal</th>
            <td>{{ meal_plan.user.goal ?? 'Not specified' }}</td>  <!-- Changed from mealPlan to meal_plan -->
        </tr>
        <tr>
            <th>Generated On</th>
            <td>{{ "now"|date("F j, Y") }}</td>
        </tr>
    </table>

    <h2>Meals in Plan</h2>

    {% if meal_plan.meals is not empty %}  <!-- Changed from mealPlan to meal_plan -->
        {% for meal in meal_plan.meals %}  <!-- Changed from mealPlan to meal_plan -->
            <div class="meal-section">
                <h2>{{ meal.title }}</h2>

                {% if meal.type %}
                    <div style="background-color: #1abc9c; color: white; padding: 5px 10px; border-radius: 4px; display: inline-block; margin-bottom: 10px; font-size: 14px;">
                        {{ meal.type|title }}
                    </div>
                {% endif %}

                <div class="meal-info">
                    <p><strong>Calories:</strong> {{ meal.calories }} kcal</p>

                    {% if meal.description %}
                        <p><strong>Description:</strong> {{ meal.description }}</p>
                    {% endif %}

                    {% if meal.type %}
                        <p><strong>Meal Type:</strong> {{ meal.type|title }}</p>
                    {% endif %}

                    <p><strong>Created:</strong> {{ meal.createdAt ? meal.createdAt|date('F j, Y') : 'Not specified' }}</p>
                </div>
            </div>
        {% endfor %}
    {% else %}
        <div class="meal-section">
            <p style="text-align: center; color: #666; font-style: italic;">
                No meals have been added to this plan yet.
            </p>
        </div>
    {% endif %}
</div>

<div class="footer">
    <p>Generated by FitFlow Nutrition System</p>
    <p>{{ "now"|date("F j, Y \\a\\t g:i A") }}</p>
    <p>© {{ "now"|date("Y") }} FitFlow. All rights reserved.</p>
</div>
</body>
</html>
