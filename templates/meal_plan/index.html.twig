<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Meal Plans - FitFlow</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&family=Montserrat:wght@400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --food-gradient: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
            --health-gradient: linear-gradient(135deg, #84fab0 0%, #8fd3f4 100%);
            --energy-gradient: linear-gradient(135deg, #a1c4fd 0%, #c2e9fb 100%);
            --protein-gradient: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);
            --veggie-gradient: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%);
            --dark-bg: #0f172a;
            --light-text: #f8fafc;
            --card-bg: rgba(255, 255, 255, 0.95);
            --shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(-45deg, #ff9a9e, #fad0c4, #a1c4fd, #c2e9fb);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
            min-height: 100vh;
            color: #333;
            padding-bottom: 60px;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Header Styles */
        .header {
            text-align: center;
            padding: 40px 20px;
            position: relative;
            overflow: hidden;
        }

        .header h1 {
            font-family: 'Montserrat', sans-serif;
            font-size: 3.5rem;
            background: var(--food-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 15px;
            text-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .header-description {
            font-size: 1.2rem;
            color: var(--light-text);
            opacity: 0.9;
            max-width: 600px;
            margin: 0 auto 30px;
            text-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
        }

        /* Hero Stats */
        .hero-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin: 40px 0;
        }

        .hero-stat {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 25px;
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: var(--transition);
        }

        .hero-stat:hover {
            transform: translateY(-5px);
            background: rgba(255, 255, 255, 0.2);
        }

        .hero-stat-icon {
            font-size: 2.5rem;
            margin-bottom: 15px;
            color: white;
        }

        .hero-stat-value {
            font-size: 2rem;
            font-weight: 700;
            color: white;
            margin-bottom: 5px;
        }

        .hero-stat-label {
            color: rgba(255, 255, 255, 0.9);
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* Button Styles */
        .btn {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            padding: 16px 32px;
            border-radius: 15px;
            text-decoration: none;
            font-weight: 600;
            transition: var(--transition);
            border: none;
            cursor: pointer;
            font-size: 1.1rem;
            justify-content: center;
        }

        .btn-primary {
            background: var(--health-gradient);
            color: #1e293b;
            box-shadow: 0 5px 15px rgba(132, 250, 176, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 25px rgba(132, 250, 176, 0.4);
        }

        .btn-danger {
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
            color: white;
            box-shadow: 0 5px 15px rgba(255, 107, 107, 0.3);
        }

        .btn-danger:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 25px rgba(255, 107, 107, 0.4);
        }

        .btn-secondary {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            color: white;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-3px);
        }

        /* Meal Plan Grid */
        .meal-plan-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 30px;
            margin-top: 40px;
        }

        .meal-plan-card {
            background: var(--card-bg);
            backdrop-filter: blur(10px);
            border-radius: 25px;
            padding: 30px;
            box-shadow: var(--shadow);
            transition: var(--transition);
            border: 1px solid rgba(255, 255, 255, 0.2);
            position: relative;
            overflow: hidden;
        }

        .meal-plan-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: var(--food-gradient);
        }

        .meal-plan-card:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
        }

        .plan-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 20px;
        }

        .plan-title {
            font-family: 'Montserrat', sans-serif;
            font-size: 1.8rem;
            font-weight: 700;
            color: #2d3748;
            line-height: 1.2;
            flex: 1;
        }

        .plan-day {
            background: var(--protein-gradient);
            color: #1e293b;
            padding: 8px 18px;
            border-radius: 20px;
            font-weight: 600;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        /* Calorie Display */
        .calorie-display {
            display: flex;
            align-items: center;
            gap: 15px;
            background: rgba(132, 250, 176, 0.1);
            padding: 20px;
            border-radius: 15px;
            margin: 20px 0;
        }

        .calorie-icon {
            width: 50px;
            height: 50px;
            background: var(--health-gradient);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            color: #1e293b;
        }

        .calorie-content h4 {
            font-size: 0.9rem;
            color: #64748b;
            margin-bottom: 5px;
        }

        .calorie-value {
            font-size: 2rem;
            font-weight: 700;
            color: #10b981;
        }

        .calorie-unit {
            font-size: 1rem;
            color: #94a3b8;
            margin-left: 5px;
        }

        /* Meal Summary - FIXED FOR YOUR ENTITY */
        .meal-summary {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
            margin: 25px 0;
        }

        .meal-item {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 15px;
            background: #f8fafc;
            border-radius: 12px;
            transition: var(--transition);
        }

        .meal-item:hover {
            background: #e2e8f0;
            transform: translateX(5px);
        }

        .meal-icon {
            width: 40px;
            height: 40px;
            background: var(--veggie-gradient);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #1e293b;
            font-size: 1.2rem;
        }

        .meal-info h4 {
            font-size: 0.85rem;
            color: #64748b;
            margin-bottom: 3px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .meal-info p {
            font-size: 1.1rem;
            font-weight: 600;
            color: #1e293b;
        }

        /* Action Buttons */
        .action-buttons {
            display: flex;
            gap: 12px;
            margin-top: 25px;
        }

        .btn-small {
            padding: 12px 24px;
            font-size: 0.95rem;
            flex: 1;
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 80px 20px;
            background: var(--card-bg);
            border-radius: 25px;
            backdrop-filter: blur(10px);
            margin-top: 40px;
            border: 2px dashed rgba(132, 250, 176, 0.3);
        }

        .empty-icon {
            font-size: 5rem;
            margin-bottom: 30px;
            background: var(--food-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .empty-state h2 {
            font-family: 'Montserrat', sans-serif;
            font-size: 2.2rem;
            margin-bottom: 15px;
            color: #2d3748;
        }

        .empty-state p {
            color: #64748b;
            font-size: 1.2rem;
            max-width: 500px;
            margin: 0 auto 40px;
            line-height: 1.6;
        }

        /* Meal Count Badge */
        .meal-count {
            position: absolute;
            top: 25px;
            right: 25px;
            background: var(--energy-gradient);
            color: #1e293b;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 1.1rem;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .header h1 {
                font-size: 2.5rem;
            }

            .meal-plan-grid {
                grid-template-columns: 1fr;
            }

            .hero-stats {
                grid-template-columns: 1fr;
            }

            .action-buttons {
                flex-direction: column;
            }

            .container {
                padding: 15px;
            }

            .meal-summary {
                grid-template-columns: 1fr;
            }
        }

        /* Animation */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .meal-plan-card {
            animation: fadeInUp 0.6s ease-out;
        }

    </style>
</head>
<body>
<div class="container">
    <div class="header">
        <h1>Your Nutrition Journey</h1>
        <p class="header-description">Discover delicious, healthy meal plans tailored to your fitness goals</p>

        <!-- REMOVED THE GENERATE BUTTON FROM HERE -->
    </div>

    {% if meal_plans is empty %}
        <div class="empty-state">
            <div class="empty-icon">
                <i class="fas fa-utensils"></i>
            </div>
            <h2>No Meal Plans Available</h2>
            <p>You don't have any meal plans yet. Meal plans are automatically generated for you based on your fitness goals and preferences.</p>
            <!-- REMOVED THE GENERATE BUTTON FROM EMPTY STATE TOO -->
        </div>
    {% else %}
        <!-- Stats Dashboard -->
        <div class="hero-stats">
            <div class="hero-stat">
                <div class="hero-stat-icon">
                    <i class="fas fa-calendar-alt"></i>
                </div>
                <div class="hero-stat-value">{{ meal_plans|length }}</div>
                <div class="hero-stat-label">Total Plans</div>
            </div>

            <div class="hero-stat">
                <div class="hero-stat-icon">
                    <i class="fas fa-fire"></i>
                </div>
                <div class="hero-stat-value">
                    {{ (meal_plans|reduce((total, plan) => total + plan.totalCalories, 0) / meal_plans|length)|round }}
                </div>
                <div class="hero-stat-label">Avg Calories</div>
            </div>

            <div class="hero-stat">
                <div class="hero-stat-icon">
                    <i class="fas fa-utensil-spoon"></i>
                </div>
                <div class="hero-stat-value">
                    {{ meal_plans|reduce((total, plan) => total + (plan.meals|length), 0) }}
                </div>
                <div class="hero-stat-label">Total Meals</div>
            </div>

            <div class="hero-stat">
                <div class="hero-stat-icon">
                    <i class="fas fa-heart"></i>
                </div>
                <div class="hero-stat-value">
                    {% set uniqueDays = [] %}
                    {% for plan in meal_plans %}
                        {% if plan.day not in uniqueDays %}
                            {% set uniqueDays = uniqueDays|merge([plan.day]) %}
                        {% endif %}
                    {% endfor %}
                    {{ uniqueDays|length }}
                </div>
                <div class="hero-stat-label">Days Covered</div>
            </div>
        </div>

        <!-- Meal Plans Grid -->
        <div class="meal-plan-grid">
            {% for meal_plan in meal_plans|reverse %}
                <div class="meal-plan-card">
                    <div class="meal-count">
                        {{ meal_plan.meals|length }}
                    </div>

                    <div class="plan-header">
                        <h2 class="plan-title">{{ meal_plan.title }}</h2>
                        <span class="plan-day">{{ meal_plan.day }}</span>
                    </div>

                    <div class="calorie-display">
                        <div class="calorie-icon">
                            <i class="fas fa-fire"></i>
                        </div>
                        <div class="calorie-content">
                            <h4>Total Calories</h4>
                            <div class="calorie-value">
                                {{ meal_plan.totalCalories }}
                                <span class="calorie-unit">kcal</span>
                            </div>
                        </div>
                    </div>

                    <!-- FIXED: Use meal.title instead of meal.name -->
                    <div class="meal-summary">
                        {% set sampleMeals = meal_plan.meals|slice(0, 4) %}
                        {% for meal in sampleMeals %}
                            <div class="meal-item">
                                <div class="meal-icon">
                                    {% if loop.index == 1 %}
                                        <i class="fas fa-sun"></i>
                                    {% elseif loop.index == 2 %}
                                        <i class="fas fa-cloud-sun"></i>
                                    {% elseif loop.index == 3 %}
                                        <i class="fas fa-moon"></i>
                                    {% else %}
                                        <i class="fas fa-seedling"></i>
                                    {% endif %}
                                </div>
                                <div class="meal-info">
                                    <h4>Meal {{ loop.index }}</h4>
                                    <!-- FIXED LINE: Use meal.title -->
                                    <p>{{ meal.title|slice(0, 15) }}{% if meal.title|length > 15 %}...{% endif %}</p>
                                </div>
                            </div>
                        {% endfor %}
                    </div>

                    <div class="action-buttons">
                        <a href="{{ path('app_meal_plan_show', {'id': meal_plan.id}) }}" class="btn btn-secondary btn-small">
                            <i class="fas fa-eye"></i>
                            View Details
                        </a>
                        <a href="{{ path('app_meal_plan_pdf', {'id': meal_plan.id}) }}" class="btn btn-danger btn-small">
                            <i class="fas fa-file-pdf"></i>
                            Download PDF
                        </a>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
</div>
</body>
</html>
